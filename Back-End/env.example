###############################################
# .env.example (Template)
# 실비밀은 절대 커밋 금지 → Azure Key Vault 사용 권장
###############################################

# --- Server ---
PORT=4000
NODE_ENV=production

# --- CORS (쉼표 구분, 필요 도메인만) ---
# 예: 로컬 + WebApp + (선택) VMSS 도메인
CORS_ORIGINS=http://localhost:5173,https://webapp-fe-bcd00.azurewebsites.net

# --- Auth / Security ---
JWT_SECRET=REPLACE_WITH_SECURE_RANDOM_64B

# --- Database (Azure SQL) ---
DB_SERVER=your-server-name.database.windows.net
DB_DATABASE=your-database-name
DB_USER=your-db-user
DB_PASSWORD=your-db-password
DB_PORT=1433
DB_CONN_TIMEOUT_MS=60000
DB_REQUEST_TIMEOUT_MS=60000
DB_POOL_MAX=10
DB_POOL_MIN=0
DB_POOL_IDLE_TIMEOUT_MS=30000

# --- Azure Storage (Prod에서는 가능하면 키 대신 MI + RBAC) ---
AZURE_STORAGE_ACCOUNT_NAME=your-storage-account-name
AZURE_STORAGE_ACCOUNT_KEY=your-storage-account-key
AZURE_BLOB_CONTAINER_NAME=receipts-data

# --- Azure Document Intelligence ---
AZURE_DOCUMENT_INTELLIGENCE_ENDPOINT=https://your-resource.cognitiveservices.azure.com/
AZURE_DOCUMENT_INTELLIGENCE_KEY=your-document-intelligence-key
DI_MODEL_ID=prebuilt-receipt

# --- (Optional) Monitoring ---
# APPINSIGHTS_CONNECTION_STRING=your-appinsights-connection-string

# NOTE:
# 1) 로컬 개발만 .env 직접 사용
# 2) 운영은 Key Vault에서 비밀 주입(fetch-secrets.sh 등)
# 3) JWT_SECRET / DB_PASSWORD / STORAGE KEY / DI KEY 절대 커밋 금지
